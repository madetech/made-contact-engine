<h1>Listing customer_cares</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @customer_cares.each do |customer_care| %>
  <tr>
    <td><%= link_to 'Show', customer_care %></td>
    <td><%= link_to 'Edit', edit_customer_care_path(customer_care) %></td>
    <td><%= link_to 'Destroy', customer_care, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Customer care', new_customer_care_path %>
