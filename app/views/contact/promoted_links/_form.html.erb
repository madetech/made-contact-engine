<%= form_for(@promoted_link) do |f| %>
  <% if @promoted_link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@promoted_link.errors.count, "error") %> prohibited this promoted_link from being saved:</h2>

      <ul>
      <% @promoted_link.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
